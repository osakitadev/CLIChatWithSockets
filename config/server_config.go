package config

const (
	// ServerPort is the port where the server will be listening
	ServerPort = ":8080"
	// ServerProtocol is the protocol that the server will be using (eithet tcp or udp)
	Protocol = "tcp"
)
